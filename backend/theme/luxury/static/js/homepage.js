// Home Page JS (optional)

// Hero button redirect example
document.querySelector('.hero button')?.addEventListener('click', () => {
    window.location.href = '#menu'; // Change to your menu section or page
});
